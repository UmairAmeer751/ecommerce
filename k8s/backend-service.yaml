apiVersion: v1
kind: Service
metadata:
  name: backend          # Must match the proxy_pass hostname in nginx.conf
spec:
  type: ClusterIP        # Internal only — frontend accesses via Nginx proxy
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000

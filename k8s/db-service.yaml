apiVersion: v1
kind: Service
metadata:
  name: db             # Must match DATABASE_URL hostname in backend env
spec:
  type: ClusterIP      # Internal only — only backend should reach the DB
  selector:
    app: db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
